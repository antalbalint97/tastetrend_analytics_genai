# Core data processing
pandas
pyarrow
s3fs

# AWS + OpenSearch integration
boto3
requests
requests-aws4auth
opensearch-py

# Optional but recommended (for logging, retrying, etc.)
tenacity
tqdm

# If you're using Bedrock embeddings
botocore
awscli

# Local dev & debugging (can be commented out in Lambda layers)
python-dotenv
